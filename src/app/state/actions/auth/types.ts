const authActionsPrefix = '[Auth]';

export const AUTH_SET_TOKEN = `${authActionsPrefix} - Set token`;
export const AUTH_RESET_TOKEN  = `${authActionsPrefix} - Reset token`;

export const AUTHENTICATE = `${authActionsPrefix} - Authenticate`;
